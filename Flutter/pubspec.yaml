name: esp_health_monitor

description: ESP Health Monitoring System Mobile Application

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  http: ^1.2.2
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.3
  connectivity_plus: ^6.0.5
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
